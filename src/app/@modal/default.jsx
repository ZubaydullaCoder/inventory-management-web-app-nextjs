// src/app/@modal/default.jsx
/**
 * Default slot content for when no modal is active
 * Required for parallel routes to function properly
 * @returns {null}
 */
export default function DefaultModal() {
  return null;
}
